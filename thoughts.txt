1. THOUGHT PROCESS BEFORE STARTING:

1.1. Read the text.
1.2. Asked Claude to explain it to me again so I know I understood it.
1.3. Clear limitations of the tool as of right now:
    - Strings might not fit UI components and might require rethinking by a human.
    - The tool can't be too simple otherwise it would already be implemented and this wouldn't be an issue.
1.4. Prioritize major issues like missing strings or hardcoded text "%@", perhaps when showing what changed, flag these in red.
1.5. Do NOT oveflag issues, if anything I'd rather the tool showed me the most egregious problems.
1.6. I'm thinking CLI for now.  

2. DEVELOPMENT PHASE:

2.1. created summary version, extended version, but very verbose and gimmicky to use

Known blind spot worth putting in the README
tk is Turkmen. The strings are unmistakably Turkish, which is tr. I chose not to flag it: it's a valid ISO 639-1 code, so catching it needs actual language detection, and it's pre-existing in the live file rather than something this release introduced. Same class of bug as jp, but not this release's problem.
make it work, make it pretty

things are flagged once, so a blocker can have other underlying issues but arent flagged

run it with no arguments and it takes the latest 2

QoL changes: defaults to show summary

3. WHAT I'D DO WITH MORE TIME/RESOURCES:

3.1. If I could somehow see the size of the components so I could instantly know if a string will overflow.
3.2 I dont know whats in production so I dont know what to compare, assuming we are working like 2 versions ahead or something.




#####

AI USAGE:

help choose tool (i prefer cli)
install dependencies
build layout/appearance
ai did not do a great job at making this human friendly
write tests, check edge cases